<%= form_with(model: tweet) do |form| %>
  <% if tweet.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
        <% tweet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :twitter_user_id, style: "display: block" %>
    <%= form.text_field :twitter_user_id %>
  </div>

  <div>
    <%= form.label :full_text, style: "display: block" %>
    <%= form.text_field :full_text %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
