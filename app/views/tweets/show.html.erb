<p style="color: green"><%= notice %></p>

<%= render @tweet %>

<div>
  <%= link_to "Edit this tweet", edit_tweet_path(@tweet) if mutations_allowed? %> |
  <%= link_to "Back to tweets", tweets_path %>
  <%= button_to "Destroy this tweet", @tweet, method: :delete if mutations_allowed? %>
</div>

